---
title: Logo Clouds
description: Trust indicators and social proof using customer logo grids.
category: Marketing
---

import { LogosSimple, LogosTicker, LogosSplit, LogosDark, LogosBordered, LogosMonochrome, LogosHeading, LogosCards, LogosQuote, LogosAnimated } from '@/components/ui-component/logos';

# Logo Clouds

Building trust is essential. Displaying the logos of your partners, customers, or integrations is a proven way to increase credibility.

## Simple Grid
Classic, clean layout for 5-6 logos.

<div className="my-8 border rounded-lg overflow-hidden">
  <LogosSimple />
</div>

```tsx
import { LogosSimple } from '@/components/ui-component/logos';

export default function Example() {
  return <LogosSimple />;
}
```

## Infinite Ticker
Smooth scrolling marquee animation. Great for showing a large volume of partners.

<div className="my-8 border rounded-lg overflow-hidden">
  <LogosTicker />
</div>

```tsx
import { LogosTicker } from '@/components/ui-component/logos';

export default function Example() {
  return <LogosTicker />;
}
```

## Split Layout
Logos paired with a headline or introductory text.

<div className="my-8 border rounded-lg overflow-hidden">
  <LogosSplit />
</div>

```tsx
import { LogosSplit } from '@/components/ui-component/logos';

export default function Example() {
  return <LogosSplit />;
}
```

## Dark Mode
High contrast logo strip for dark themed sections.

<div className="my-8 border rounded-lg overflow-hidden">
  <LogosDark />
</div>

```tsx
import { LogosDark } from '@/components/ui-component/logos';

export default function Example() {
  return <LogosDark />;
}
```

## Bordered Grid
Structured grid with dividers.

<div className="my-8 border rounded-lg overflow-hidden">
  <LogosBordered />
</div>

```tsx
import { LogosBordered } from '@/components/ui-component/logos';

export default function Example() {
  return <LogosBordered />;
}
```

## Monochrome Brand Color
Logos tinted to match your brand's primary color.

<div className="my-8 border rounded-lg overflow-hidden">
  <LogosMonochrome />
</div>

```tsx
import { LogosMonochrome } from '@/components/ui-component/logos';

export default function Example() {
  return <LogosMonochrome />;
}
```

## Centered Heading
Focus attention on the trust statement.

<div className="my-8 border rounded-lg overflow-hidden">
  <LogosHeading />
</div>

```tsx
import { LogosHeading } from '@/components/ui-component/logos';

export default function Example() {
  return <LogosHeading />;
}
```

## Card Grid
Each logo in its own card.

<div className="my-8 border rounded-lg overflow-hidden">
  <LogosCards />
</div>

```tsx
import { LogosCards } from '@/components/ui-component/logos';

export default function Example() {
  return <LogosCards />;
}
```

## With Quote
Featured partner testimonial alongside logos.

<div className="my-8 border rounded-lg overflow-hidden">
  <LogosQuote />
</div>

```tsx
import { LogosQuote } from '@/components/ui-component/logos';

export default function Example() {
  return <LogosQuote />;
}
```

## Animated Entrance
Logos that fade in sequentially.

<div className="my-8 border rounded-lg overflow-hidden">
  <LogosAnimated />
</div>

```tsx
import { LogosAnimated } from '@/components/ui-component/logos';

export default function Example() {
  return <LogosAnimated />;
}
```
