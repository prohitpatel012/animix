---
title: Stats & Metrics
description: Showcase your numbers, growth, and reliability.
category: Marketing
---

import { StatsSimple, StatsDark, StatsCards, StatsSplit, StatsGrid, StatsTimeline, StatsComparison, StatsAnimated, StatsBackground, StatsVertical } from '@/components/ui-component/stats';

# Stats & Metrics

Numbers speak louder than words. Use these components to validate your claims, show scale, and demonstrate growth.

## Simple
Clean, large numbers.

<div className="my-8 border rounded-lg overflow-hidden">
  <StatsSimple />
</div>

```tsx
import { StatsSimple } from '@/components/ui-component/stats';

export default function Example() {
  return <StatsSimple />;
}
```

## Dark Mode
Stats on a dark background for contrast.

<div className="my-8 border rounded-lg overflow-hidden">
  <StatsDark />
</div>

```tsx
import { StatsDark } from '@/components/ui-component/stats';

export default function Example() {
  return <StatsDark />;
}
```

## Cards with Icons
Detailed stats in a grid of cards.

<div className="my-8 border rounded-lg overflow-hidden">
  <StatsCards />
</div>

```tsx
import { StatsCards } from '@/components/ui-component/stats';

export default function Example() {
  return <StatsCards />;
}
```

## Split with Image
Contextual stats alongside visuals.

<div className="my-8 border rounded-lg overflow-hidden">
  <StatsSplit />
</div>

```tsx
import { StatsSplit } from '@/components/ui-component/stats';

export default function Example() {
  return <StatsSplit />;
}
```

## Dashboard Grid
Compact, data-dense layout.

<div className="my-8 border rounded-lg overflow-hidden">
  <StatsGrid />
</div>

```tsx
import { StatsGrid } from '@/components/ui-component/stats';

export default function Example() {
  return <StatsGrid />;
}
```

## Timeline Growth
Showing progression over time.

<div className="my-8 border rounded-lg overflow-hidden">
  <StatsTimeline />
</div>

```tsx
import { StatsTimeline } from '@/components/ui-component/stats';

export default function Example() {
  return <StatsTimeline />;
}
```

## Comparison
Head-to-head metrics against competitors.

<div className="my-8 border rounded-lg overflow-hidden">
  <StatsComparison />
</div>

```tsx
import { StatsComparison } from '@/components/ui-component/stats';

export default function Example() {
  return <StatsComparison />;
}
```

## Large Animated
Focus on one massive, impressive number.

<div className="my-8 border rounded-lg overflow-hidden">
  <StatsAnimated />
</div>

```tsx
import { StatsAnimated } from '@/components/ui-component/stats';

export default function Example() {
  return <StatsAnimated />;
}
```

## Cyber / Technical
Stats overlaying a technical background.

<div className="my-8 border rounded-lg overflow-hidden">
  <StatsBackground />
</div>

```tsx
import { StatsBackground } from '@/components/ui-component/stats';

export default function Example() {
  return <StatsBackground />;
}
```

## Vertical List
Stats emphasized in a side-by-side layout.

<div className="my-8 border rounded-lg overflow-hidden">
  <StatsVertical />
</div>

```tsx
import { StatsVertical } from '@/components/ui-component/stats';

export default function Example() {
  return <StatsVertical />;
}
```
