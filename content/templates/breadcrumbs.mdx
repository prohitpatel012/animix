---
title: Breadcrumbs
description: Navigation trails to show current location.
category: Navigation
---

import { BreadcrumbSimple, BreadcrumbChevron, BreadcrumbBackground, BreadcrumbHomeIcon, BreadcrumbCollapsed, BreadcrumbFullWidth, BreadcrumbArrow, BreadcrumbDark, BreadcrumbGlass, BreadcrumbMinimal } from '@/components/ui-component/breadcrumbs';

# Breadcrumbs

Help users understand their location within your website's hierarchy.

## Simple Slash
Basic navigation with slash separators.

<div className="my-8 border rounded-lg overflow-hidden">
  <BreadcrumbSimple />
</div>

```tsx
import { BreadcrumbSimple } from '@/components/ui-component/breadcrumbs';

export default function Example() {
  return <BreadcrumbSimple />;
}
```

## With Chevrons
Standard chevron icons for direction.

<div className="my-8 border rounded-lg overflow-hidden">
  <BreadcrumbChevron />
</div>

```tsx
import { BreadcrumbChevron } from '@/components/ui-component/breadcrumbs';

export default function Example() {
  return <BreadcrumbChevron />;
}
```

## Contained Background
Wrapped in a subtle container box.

<div className="my-8 border rounded-lg overflow-hidden">
  <BreadcrumbBackground />
</div>

```tsx
import { BreadcrumbBackground } from '@/components/ui-component/breadcrumbs';

export default function Example() {
  return <BreadcrumbBackground />;
}
```

## Home Icon
Starting with a visual home anchor.

<div className="my-8 border rounded-lg overflow-hidden">
  <BreadcrumbHomeIcon />
</div>

```tsx
import { BreadcrumbHomeIcon } from '@/components/ui-component/breadcrumbs';

export default function Example() {
  return <BreadcrumbHomeIcon />;
}
```

## Collapsed
Handling long paths with ellipsis.

<div className="my-8 border rounded-lg overflow-hidden">
  <BreadcrumbCollapsed />
</div>

```tsx
import { BreadcrumbCollapsed } from '@/components/ui-component/breadcrumbs';

export default function Example() {
  return <BreadcrumbCollapsed />;
}
```

## Full Width Bar
Edge-to-edge container commonly found in admin panels.

<div className="my-8 border rounded-lg overflow-hidden">
  <BreadcrumbFullWidth />
</div>

```tsx
import { BreadcrumbFullWidth } from '@/components/ui-component/breadcrumbs';

export default function Example() {
  return <BreadcrumbFullWidth />;
}
```

## Arrow Steps
Visual progress tracker style.

<div className="my-8 border rounded-lg overflow-hidden">
  <BreadcrumbArrow />
</div>

```tsx
import { BreadcrumbArrow } from '@/components/ui-component/breadcrumbs';

export default function Example() {
  return <BreadcrumbArrow />;
}
```

## Dark Mode
Optimized for dark interfaces.

<div className="my-8 border rounded-lg overflow-hidden">
  <BreadcrumbDark />
</div>

```tsx
import { BreadcrumbDark } from '@/components/ui-component/breadcrumbs';

export default function Example() {
  return <BreadcrumbDark />;
}
```

## Glassmorphism
Modern translucent floating bar.

<div className="my-8 border rounded-lg overflow-hidden">
  <BreadcrumbGlass />
</div>

```tsx
import { BreadcrumbGlass } from '@/components/ui-component/breadcrumbs';

export default function Example() {
  return <BreadcrumbGlass />;
}
```

## Minimalist
Small text with arrow icons.

<div className="my-8 border rounded-lg overflow-hidden">
  <BreadcrumbMinimal />
</div>

```tsx
import { BreadcrumbMinimal } from '@/components/ui-component/breadcrumbs';

export default function Example() {
  return <BreadcrumbMinimal />;
}
```
